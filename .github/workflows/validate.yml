name: Validate Registry
on:
  push:
    branches: [main, '7.x']
  pull_request:
    branches: [main, '7.x']
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pre-commit/action@v3.0.0
